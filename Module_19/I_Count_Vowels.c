#include <stdio.h>
#include <string.h>


void vowel_check(char arr[201])
{

    if(arr == )

}

int main() {
    char arr[201];
    fgets(arr,201,stdin);
    int length;
    length = strlen(arr);


    return 0;
}